import { Metadata } from 'next';
import { getMetadata } from './metadata.utils';
import { PAGES_METADATA } from '@/constants/metadata';

export type { Metadata };
export { getMetadata, PAGES_METADATA };
